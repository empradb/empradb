{
  "name": "empradb",
  "version": "0.1.0",
  "private": true,
  "description": "The World's Largest Math Database & Study Hub",
  "repository": {
    "type": "git",
    "url": "https://github.com/empradb/empradb"
  },
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "validate:graph": "tsx scripts/validate_graph.ts",
    "ingest:math": "tsx scripts/ingest_math.ts",
    "build:index": "tsx scripts/build_index.ts"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.3.0",
    "tsx": "^4.7.0",
    "@types/node": "^20.10.0",
    "prettier": "^3.1.0",
    "eslint": "^8.55.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
